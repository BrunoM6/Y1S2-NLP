# Natural Language Processing HAM SPAM PHISHING Detection

Made by: Bruno Moreira, Tiago Gonçalves, Tomás Vinhas.

## Setup Instructions

1. Create a folder named `data` in the root directory of the project.

2. Place the dataset files inside the `data` folder.

3. Install the required dependencies by running:

```bash
pip install -r requirements.txt
